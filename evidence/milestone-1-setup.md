# Milestone 1: Base VM and Repository Setup

Capture from the local Ubuntu VM:

```bash
cat /etc/os-release
pwd
find . -maxdepth 3 -type f | sort
sudo ./scripts/provision.sh
```

Expected evidence:

- OS/version screenshot
- repository structure screenshot
- first successful provisioning run screenshot or terminal log
